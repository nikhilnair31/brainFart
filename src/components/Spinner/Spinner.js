import React, {  } from 'react';
import './Spinner.scss';

const Spinner = () => {
	
    return(
        <div className="spinner_container">
            <div className="spinner"/>
        </div>
    );
}

export default Spinner;